[train]
batch_size = 32
num_epochs = 100
log_path = /opt/analysis/leveeNet/log/
split = 0.8

[model]
n_classes = 1
model_outpath = /opt/analysis/leveeNet/final.hdf5

[generator]
image_size = (252, 252)  # tuple(h, w)
max_pool = 2  # maxPooling before resizing
shuffle = False  # shuffle batch index per epoch?
augment = True  # perform data augumentation?

[data]
data_path = /share/host/Dataset/data.nc

